<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bandwidth.adhiksh">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".AuthorityHomepage"></activity>
        <activity android:name=".FormSubmission" />
        <activity android:name=".CitizenHomepage"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".AuthorityHome" />
        <activity android:name=".FirForm2" />
        <activity android:name=".FirForm" />
        <activity android:name=".ComplaintForm" />
        <activity android:name=".AppointmentForm" />
        <activity android:name=".NOCForm" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>